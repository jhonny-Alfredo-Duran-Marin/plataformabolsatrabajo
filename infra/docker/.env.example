DB_PASSWORD=postgres
JWT_SECRET=change-me-min-32-characters-please
CORS_ORIGINS=["http://localhost:4200","http://localhost"]
BACKEND_IMAGE=egresa-backend
FRONTEND_IMAGE=egresa-frontend

# Opcional: si se define, el backend usa esta base en vez del Postgres local
# del compose (ej. para conectarse a la Supabase compartida del equipo).
# Copia el valor real de DATABASE_URL desde backend/.env.
# DATABASE_URL=
